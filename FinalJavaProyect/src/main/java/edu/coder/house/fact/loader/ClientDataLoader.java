////package edu.coder.house.fact.loader;


import edu.coder.house.fact.entity.Client;
import edu.coder.house.fact.service.ClientService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.CommandLineRunner;
import org.springframework.stereotype.Component;

////@Component
////public class ClientDataLoader implements CommandLineRunner {

//// private final ClientService clientService;

    //// @Autowired
    ////  public ClientDataLoader(ClientService clientService) {
    ////     this.clientService = clientService;
    ////  }

   //// public void run(String... args) throws Exception {
        // Cargar 10 clientes de ejemplo
       // for (int i = 1; i <= 10; i++) {
          //  Client client = new Client();
            // Eliminar esta línea
            // client.setId(UUID.randomUUID()); // Generar un UUID único
         //   client.setName("Client Name " + i); // Nombre del cliente
       //     client.setLastname("Client Lastname " + i); // Apellido del cliente
       //     client.setDni(String.format("%08d", i * 12345)); // Ejemplo de DNI
        //    client.setEmail("client" + i + "@example.com"); // Correo del cliente
          //  client.setTelefono("123456789" + i); // Teléfono del cliente

           // clientService.save(client); // Guardar el cliente en la base de datos
//// }
                ////}


////}